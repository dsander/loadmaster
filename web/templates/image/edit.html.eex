<h2>Edit image</h2>

<%= render "form.html", changeset: @changeset,
                        action: repository_image_path(@conn, :update, @repository, @image) %>

<%= link "Back", to: repository_image_path(@conn, :index, @repository) %>
