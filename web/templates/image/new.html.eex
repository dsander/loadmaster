<h2>New image</h2>

<%= render "form.html", changeset: @changeset,
                        action: repository_image_path(@conn, :create, @repository) %>

<%= link "Back", to: repository_image_path(@conn, :index, @repository) %>
